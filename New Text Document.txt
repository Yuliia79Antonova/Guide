Git branch - Users can create a new branch in Git by running the 'git checkout -b my-branch-name' command, where 'my-branch-name' can be replaced with the desired name. To switch between local branches, the 'git checkout my-branch-name' command is used.
 

